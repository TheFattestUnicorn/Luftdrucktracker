import { open as openI2cBus } from 'i2c-bus';
import BusInterface from './BusInterface';
declare const Bus: ({ busNumber, openBus }?: {
    busNumber?: number | undefined;
    openBus?: typeof openI2cBus | undefined;
}) => BusInterface;
export default Bus;
