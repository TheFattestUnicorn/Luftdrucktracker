import BusInterface from './BusInterface';
import DeviceInterface from './DeviceInterface';
declare const Device: ({ address, bus }: {
    address: number;
    bus: BusInterface;
}) => DeviceInterface;
export default Device;
